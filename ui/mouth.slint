export component Mouth inherits Rectangle {
    width: 600px;
    height: self.width / 2;
    clip: true;

    Rectangle {
        y: -self.height / 2;
        width: 100%;
        height: 200%;
        border-radius: self.height / 2;
        background: black;
    }
}